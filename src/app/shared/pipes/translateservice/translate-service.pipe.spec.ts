import { TranslateServicePipe } from './translate-service.pipe';

describe('TranslateServicePipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateServicePipe();
    expect(pipe).toBeTruthy();
  });
});
